# D-BOOST – Environment variables for build & deploy
# Copy to .env.local for local dev. On Vercel, set these in Project → Settings → Environment Variables.

# Public site URL (used for SEO, sitemap, canonical, OG images). No trailing slash.
# Vercel sets VERCEL_URL automatically; override with your custom domain URL for production.
NEXT_PUBLIC_SITE_URL=https://d-boost.vercel.app

# Optional: Google Analytics (e.g. G-XXXXXXXXXX). Leave empty to disable.
# NEXT_PUBLIC_GA_ID=

# Optional: Web3Forms access key for Early Access form. Get one at https://web3forms.com
# NEXT_PUBLIC_WEB3FORMS_KEY=
